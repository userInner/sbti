version: '3.8'

services:
  sbti-app:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: sbti-server
    restart: always
    ports:
      - "9999:3000"
    volumes:
      # 挂载数据目录，确保统计数据持久化
      - sbti-data:/app/data

volumes:
  sbti-data:
